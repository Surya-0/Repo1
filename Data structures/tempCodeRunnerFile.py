graph = Graph()
            # for city in cities:
            #     graph.add_vertices(city)
            # for flight in flights:
            #     if city[0] >= mid and city[1] >= mid:
            #         graph.add_edges(flight)
            